name: docker-pdf-autonamer

services:
  watcher:
    build:
      context: .
      dockerfile: Dockerfile
      target: watcher
    environment:
      OLLAMA_MODEL: ${OLLAMA_MODEL:-qwen3:4b}
      MAX_PROCESS_ATTEMPTS: ${MAX_PROCESS_ATTEMPTS:-3}
      FAILED_RETRY_DELAY_SECONDS: ${FAILED_RETRY_DELAY_SECONDS:-300}
    volumes:
      - type: bind
        source: ${WATCH_DIR_HOST:-${HOME}/Library/Mobile Documents/com~apple~CloudDocs/docs}
        target: /watch
    depends_on:
      surya:
        condition: service_healthy
      ollama-pull:
        condition: service_completed_successfully
    restart: unless-stopped

  surya:
    build:
      context: .
      dockerfile: Dockerfile
      target: surya-service
    mem_limit: ${SURYA_MEM_LIMIT:-8g}
    memswap_limit: ${SURYA_MEMSWAP_LIMIT:-12g}
    volumes:
      - type: bind
        source: ${WATCH_DIR_HOST:-${HOME}/Library/Mobile Documents/com~apple~CloudDocs/docs}
        target: /watch
        read_only: true
      - type: volume
        source: surya-cache
        target: /root/.cache
    environment:
      SURYA_RENDERED_FALLBACK_MAX_DIMENSION: ${SURYA_RENDERED_FALLBACK_MAX_DIMENSION:-1600}
      SURYA_DIRECT_OCR_MAX_PAGE_DIMENSION_POINTS: ${SURYA_DIRECT_OCR_MAX_PAGE_DIMENSION_POINTS:-1600}
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8077/health', timeout=5).read()",
        ]
      interval: 15s
      timeout: 10s
      retries: 20
      start_period: 30s
    restart: unless-stopped

  ollama:
    image: ollama/ollama:latest
    volumes:
      - ollama:/root/.ollama
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 10s
      timeout: 10s
      retries: 30
      start_period: 10s
    restart: unless-stopped

  ollama-pull:
    image: ollama/ollama:latest
    environment:
      OLLAMA_HOST: http://ollama:11434
    entrypoint: ["ollama"]
    command: ["pull", "${OLLAMA_MODEL:-qwen3:4b}"]
    depends_on:
      ollama:
        condition: service_healthy
    restart: "no"

volumes:
  ollama:
  surya-cache:
